import type { CollectionConfig } from 'payload'

export const Blocks: CollectionConfig = {
  slug: 'blocks',
  //   admin: {
  //     useAsTitle: 'email',
  //   },
  auth: true,
  fields: [
    // {
    //   name: 'slug',
    //   type: 'text',
    // },
  ],
}
